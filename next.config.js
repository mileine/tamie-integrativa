/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // app directory is used by default in Next 14 — no experimental flags needed here
}

module.exports = nextConfig
